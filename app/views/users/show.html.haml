%body
  .header
    %h1.header-nickname
      = @nickname
  .main
    .wrapper
      .contents
        .content
          %h2 投稿一覧
          .search
            %input.search__input{name: "keyword", placeholder: "投稿記事を検索", type: "text", value: ""}
            %button.search__btn{type: "submit", title: "検索", type: "hidden"} 検索
          .content__box
            - @messages.each do |message|
              =render partial: 'messages/posts-list', locals: {message: message}
          = paginate(@messages)
          = link_to '戻る', "/messages/"